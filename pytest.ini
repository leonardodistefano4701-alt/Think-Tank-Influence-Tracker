[pytest]
testpaths = data-pipeline/tests scripts/tests
python_files = test_*.py
# asyncio_mode is intentionally omitted: there are no async tests yet, and
# setting it without pytest-asyncio installed emits an unknown-option warning.
